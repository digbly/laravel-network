# Base URL of the JSON API (proxied by Vite in dev).
VITE_API_BASE_URL=/api/v1

# Base URL of the Laravel/Passport OAuth server.
VITE_OAUTH_BASE_URL=http://localhost:8000

# Public PKCE client id from `php artisan passport:client --public`.
VITE_OAUTH_CLIENT_ID=
